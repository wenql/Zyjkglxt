<div ng-controller="main.views.administration.roles.index as vm">
    <div class="m-subheader ">
        <div class="d-flex align-items-center">
            <div class="mr-auto">
                <h3 class="m-subheader__title m-subheader__title--separator">
                    <span>角色</span>
                </h3>
                <span class="m-section__sub">
                    使用角色进行权限分组
                </span>
            </div>
            <button class="btn btn-primary blue" style=""><i class="fa fa-plus"></i> 添加角色</button>
        </div>
    </div>
    <div class="m-content">
        <div class="m-portlet m-portlet--mobile">
            <div class="m-portlet__body">
                <div busy-if="vm.loading">
                    <div ui-grid="vm.roleGridOptions" ui-grid-resize-columns ui-grid-auto-resize ng-style="vm.getTableHeight()"></div>
                </div>
            </div>
        </div>
    </div>
</div>